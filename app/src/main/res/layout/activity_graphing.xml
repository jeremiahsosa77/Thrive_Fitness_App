<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@android:color/white">

    <!-- Top toolbar (optional, if using ActionBar/Toolbar) -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Line Chart -->
    <com.github.mikephil.charting.charts.LineChart
        android:id="@+id/workoutChart"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginTop="16dp" />

    <!-- Text-based AI Recommendation -->
    <TextView
        android:id="@+id/recommendationText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your personalized workout plan will appear here"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:paddingTop="16dp"
        android:paddingBottom="16dp" />

</LinearLayout>
strings.xml(res/values/strings.xml):
<string-array name="fitness_goals">
    <item>LEANED / TONED</item>
    <item>BUILD MUSCLE</item>
    <item>STRENGTH & POWER</item>
    <item>ATHLETIC & AGILE</item>
    <item>ENDURANCE & STAMINA</item>
    <item>GENERAL FITNESS AND HEALTH</item>
</string-array>
